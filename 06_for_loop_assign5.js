console.warn("Q.No. 1.1.1 Reverse the complete string");
function reverseString(string) {
    var worldLength=string.length-1;
    result="";
    
    for (let index = worldLength; index >=0; index--) {
        result=result+string.charAt(index)
    if (index!=" ") {
        
    }
        
    }
      console.log(result);
}
reverseString("Hard work always pays back");